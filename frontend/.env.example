# Stripe configuration (for premium payments)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...

# API base URL (optional, defaults to same origin)
# VITE_API_BASE_URL=http://localhost:5000

# Public site URL (optional, defaults to window.location.origin)
# Used for share links and redirects
# VITE_PUBLIC_SITE_URL=https://apps.kjsoft.dk
